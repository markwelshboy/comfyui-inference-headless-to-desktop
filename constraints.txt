# ================================================================
# Global indexes (critical so custom_nodes inherit the same world)
# ================================================================
--index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.org/simple

# ================================================================
# Lock torch stack (prevent nodes from pulling mismatched builds)
# ================================================================
# CUDA is selected by the index above. Keep the version pins in the
# exact installed local-version form so runtime health checks match pip freeze.
torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128
# Conservative fallback if a custom node dislikes 2.11:
# torch==2.10.0
# torchvision==0.25.0
# torchaudio==2.10.0

# ================================================================
# Core numerics (keep consistent with OpenCV + kornia + friends)
# ================================================================
numpy==2.2.6
scipy==1.17.0

# TorchSDE: avoid 0.2.5 invalid metadata with newer pip
torchsde==0.2.6
trampoline==0.1.2

# ================================================================
# CV stack
# ================================================================
opencv-python-headless==4.12.0.88
Pillow==12.1.0
einops==0.8.1
kornia==0.8.2

# ================================================================
# HF / transformers ecosystem (avoid hub 1.x surprises)
# ================================================================
huggingface-hub==0.36.0
safetensors==0.7.0
transformers==4.57.6
tokenizers==0.22.2
accelerate==1.14.0
diffusers==0.37.1
sentencepiece==0.2.1

# ================================================================
# Pull in latest pip/setuptools to avoid old versions that don't understand the new metadata formats
# ================================================================

setuptools>=66.1,<82
wheel>=0.38
