# ================================================================
# Global indexes (critical so custom_nodes inherit the same world)
# ================================================================
--index-url https://download.pytorch.org/whl/nightly/cu128
--extra-index-url https://pypi.org/simple

# ================================================================
# Lock torch stack (prevent nodes from pulling mismatched builds)
# ================================================================
torch==2.12.0.dev20260308+cu128
torchvision==0.26.0.dev20260308+cu128
torchaudio==2.11.0.dev20260308+cu128

# ================================================================
# Core numerics (keep consistent with OpenCV + kornia + friends)
# ================================================================
numpy==2.2.6
scipy==1.17.0

# TorchSDE: avoid 0.2.5 invalid metadata with newer pip
torchsde==0.2.6
trampoline==0.1.2

# ================================================================
# CV stack
# ================================================================
opencv-python-headless==4.12.0.88
Pillow==12.1.0
einops==0.8.1
kornia==0.8.2

# ================================================================
# HF / transformers ecosystem (avoid hub 1.x surprises)
# ================================================================
huggingface-hub==0.36.0
safetensors==0.7.0

# (Optional) If you frequently install nodes that bring transformers/diffusers,
# pin them too once you confirm versions that work for you:
# transformers==4.57.6
# diffusers==0.36.0
# tokenizers==0.22.2